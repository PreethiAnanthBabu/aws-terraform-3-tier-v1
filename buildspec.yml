version: 0.2

phases:
  install:
    commands:
      - echo "Install phase started"

  build:
    commands:
      - echo "Build phase started"
      - echo "Preparing deployment artifact"

artifacts:
  files:
    - '**/*'
